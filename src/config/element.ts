import Vue from 'vue'
import { Dialog, Button, Form, FormItem, Input, Menu, MenuItem, Submenu } from 'element-ui'

Vue.use(Dialog).use(Button).use(Form).use(FormItem).use(Input).use(Menu).use(MenuItem).use(Submenu)
