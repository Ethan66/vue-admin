import Vue from 'vue'
import {
  Dialog,
  Button,
  Form,
  FormItem,
  Input,
  Menu,
  MenuItem,
  Submenu,
  Tabs,
  TabPane,
  Dropdown,
  DropdownMenu,
  DropdownItem
} from 'element-ui'

Vue.use(Dialog)
  .use(Button)
  .use(Form)
  .use(FormItem)
  .use(Input)
  .use(Menu)
  .use(MenuItem)
  .use(Submenu)
  .use(Tabs)
  .use(TabPane)
  .use(Dropdown)
  .use(DropdownMenu)
  .use(DropdownItem)
